version: '3.9'

services:
  # backend:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.backend
  #   container_name: ats_backend
  #   restart: always
  #   ports:
  #     - "8000:8000"
  #   env_file:
  #     - .env
  #   depends_on:
  #     - db
  #     - minio

  db:
    image: postgres:15
    container_name: seekClarity
    restart: always
    environment:
      POSTGRES_DB: seekClarity_db
      POSTGRES_USER: seekClarity_user
      POSTGRES_PASSWORD: seekClarity_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  # minio:
  #   image: minio/minio
  #   container_name: ats_minio
  #   restart: always
  #   command: server /data --console-address ":9001"
  #   environment:
  #     MINIO_ROOT_USER: minioadmin
  #     MINIO_ROOT_PASSWORD: minioadmin
  #   ports:
  #     - "9000:9000"
  #     - "9001:9001"
  #   volumes:
  #     - minio_data:/data

volumes:
  postgres_data:
  minio_data: